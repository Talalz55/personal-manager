<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FinalProject.Views.ElementPages.ReminderPage"
             Title="Info"
             BackgroundColor="GhostWhite"
             Padding="30"
             NavigationPage.HasNavigationBar="False">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Label">
                <Setter Property="TextColor" Value="DarkSlateBlue"/>
                <Setter Property="FontSize" Value="20"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Background>
        <StaticResource Key="PageBackground" />
    </ContentPage.Background>
    <VerticalStackLayout Spacing="30">
        <Label 
            x:Name="TitleLabel"
            VerticalOptions="Center" 
            HorizontalOptions="Start" />
        <Label 
            x:Name="TextLabel"
            VerticalOptions="Center" 
            HorizontalOptions="Start" />
        <HorizontalStackLayout HorizontalOptions="Center" Spacing="20">
            <Label x:Name="DayLabel"/>
            <Label x:Name="TimeLabel"/>
        </HorizontalStackLayout>
        <Button Text="Return" CornerRadius="30" BackgroundColor="DimGray" TextColor="White" Clicked="Button_Clicked" FontSize="15"/>
    </VerticalStackLayout>
</ContentPage>